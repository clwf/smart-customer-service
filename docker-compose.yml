version: "3.8"

services:
  app:
    build: .
    ports:
      - "7860:7860"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./documents:/app/documents
      - ./chroma_db:/app/chroma_db
      - ./models:/app/models
